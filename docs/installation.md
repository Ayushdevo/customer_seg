# Installation Guide

This document provides an overview of installation guide.

## Overview

- Purpose of installation guide.
- Key considerations and best practices.

## Implementation Notes

- Describe how this applies to the customer segmentation project.

## Model compatibility

The bundled KMeans and StandardScaler artifacts were serialized with scikit-learn 1.6.1.
Install the project (`python -m pip install -e .`) to use that exact runtime.
Retrain and regenerate both artifacts before changing this dependency version.
